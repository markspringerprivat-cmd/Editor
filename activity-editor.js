(() => {
  const editorType = document.body.dataset.editor;
  const app = document.getElementById('app');
  const esc = (v) => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const uid = () => 'id-' + Math.random().toString(36).slice(2) + Date.now().toString(36);
  const clone = (x) => JSON.parse(JSON.stringify(x));

  const defaultStyle = () => ({ width:'100%', height:'auto', borderColor:'#b8c7d6', backgroundColor:'#ffffff', textColor:'#111827', fontSize:'16', fontFamily:'Inter, system-ui, sans-serif' });
  const defaultBlock = (type='text') => ({ id:uid(), type, title:typeLabel(type), text:'Hier steht dein Inhalt.', media:'', question:'Welche Antwort passt?', description:'Wähle eine Antwort aus.', answers:['Antwort 1','Antwort 2','Antwort 3'], correct:[0], dragText:'Eine professionelle Gesprächsführung braucht [Wartezeit] und [Rückmeldung].', pairs:[{item:'Wartezeit',target:'Planung'},{item:'Beispiel',target:'Verständnis'}], interactions:[{id:uid(), time:4, question:'Was ist hier wichtig?', description:'Wähle eine Antwort aus.', answers:['Genau beobachten','Überspringen'], correctIndex:0, pause:true}], style:defaultStyle() });
  const defaultPage = (n=1) => ({ id:uid(), title:`${editorType === 'book' ? 'Seite' : 'Folie'} ${n}`, blocks:[defaultBlock('text')] });
  const typeLabel = (type) => ({ text:'Text', imageText:'Bild mit Text', video:'Video', interactiveVideo:'Interaktives Video', choice:'Single & Multiple Choice', dragWords:'Drag the Words', dragDrop:'Drag and Drop' }[type] || type);

  function normalizeBlock(raw) {
    const b = { ...defaultBlock(raw?.type || 'text'), ...(raw || {}) };
    b.style = { ...defaultStyle(), ...(b.style || {}) };
    b.answers = Array.isArray(b.answers) && b.answers.length ? b.answers.map(String) : ['Antwort 1'];
    b.correct = Array.isArray(b.correct) ? b.correct.map(Number).filter(Number.isFinite) : [Number(b.correctIndex) || 0];
    b.pairs = Array.isArray(b.pairs) && b.pairs.length ? b.pairs.filter(p => p.item && p.target) : [{item:'Begriff', target:'Zielbereich'}];
    b.interactions = Array.isArray(b.interactions) ? b.interactions : [];
    return b;
  }

  function styleAttr(block) {
    const s = block.style || defaultStyle();
    const w = String(s.width || '100%');
    const h = String(s.height || 'auto');
    return `style="width:${esc(w)};height:${esc(h)};border-color:${esc(s.borderColor)};background:${esc(s.backgroundColor)};color:${esc(s.textColor)};font-size:${Number(s.fontSize)||16}px;font-family:${esc(s.fontFamily)}"`;
  }

  function renderBlockContent(block, playable=false) {
    const b = normalizeBlock(block);
    const media = (b.media || '').trim();
    if (b.type === 'imageText') return `${media ? `<img class="media" src="${esc(media)}" alt="">` : ''}<p>${esc(b.text)}</p>`;
    if (b.type === 'video') return `<p>${esc(b.text)}</p>${media ? `<video class="video" src="${esc(media)}" controls></video>` : '<p class="hint">Noch keine Video-URL eingetragen.</p>'}`;
    if (b.type === 'interactiveVideo') return renderInteractiveVideoBlock(b, playable);
    if (b.type === 'choice') return renderChoiceBlock(b, playable);
    if (b.type === 'dragWords') return renderDragWordsBlock(b, playable);
    if (b.type === 'dragDrop') return renderDragDropBlock(b, playable);
    return `<p>${esc(b.text)}</p>`;
  }

  function renderBlock(block, selectedId=null, playable=false) {
    const b = normalizeBlock(block);
    return `<article class="block blind-readable ${selectedId === b.id ? 'is-selected' : ''}" data-block-id="${esc(b.id)}" data-blind-target="h5p-lite-block" ${styleAttr(b)}>
      <div class="block-head"><span class="block-title">${esc(b.title || typeLabel(b.type))}</span>${playable ? '' : `<span class="hint">${esc(typeLabel(b.type))}</span>`}</div>
      ${renderBlockContent(b, playable)}
    </article>`;
  }

  function renderChoiceBlock(b, playable=false) {
    const multi = (b.correct || []).length > 1;
    return `<div data-run="choice"><h3>${esc(b.question || b.title)}</h3><p>${esc(b.description || '')}</p><div class="choice-stack">${b.answers.map((a,i)=>`<label class="choice-option"><input type="${multi ? 'checkbox' : 'radio'}" name="choice-${esc(b.id)}" value="${i}"> <span>${esc(a)}</span></label>`).join('')}</div><button class="btn primary check-choice spacer-bottom" type="button">Prüfen</button><div class="feedback" hidden></div></div>`;
  }

  function renderDragWordsBlock(b) {
    const words = [];
    const text = esc(b.dragText || '').replace(/\[([^\]]+)\]/g, (_, w) => { words.push(w); return `<span class="dtw-blank" data-answer="${esc(w)}"></span>`; });
    return `<div data-run="dragWords"><p class="dtw-text">${text}</p><div class="word-bank">${words.map(w=>`<button class="chip" type="button" draggable="true">${esc(w)}</button>`).join('')}</div><button class="btn primary check-dtw spacer-bottom" type="button">Prüfen</button><div class="feedback" hidden></div></div>`;
  }

  function renderDragDropBlock(b) {
    const targets = [...new Set(b.pairs.map(p => p.target))];
    return `<div data-run="dragDrop"><p>${esc(b.description || 'Ziehe die Begriffe in die passenden Felder.')}</p><div class="dnd-bank">${b.pairs.map(p=>`<button class="dnd-item" type="button" draggable="true" data-target="${esc(p.target)}">${esc(p.item)}</button>`).join('')}</div><div class="dnd-target-grid">${targets.map(t=>`<div class="dnd-target" data-target="${esc(t)}"><strong>${esc(t)}</strong></div>`).join('')}</div><button class="btn primary check-dnd spacer-bottom" type="button">Prüfen</button><div class="feedback" hidden></div></div>`;
  }

  function renderInteractiveVideoBlock(b, playable=false) {
    const media = (b.media || '').trim();
    if (!media) return '<p class="hint">Noch keine Video-URL eingetragen.</p>';
    return `<p>${esc(b.text)}</p><div class="iv-stage" data-interactions="${esc(JSON.stringify(b.interactions || []))}"><video class="video" src="${esc(media)}" controls></video><div class="glass-overlay" hidden></div></div><p class="hint">${(b.interactions || []).length} Interaktion(en) angelegt.</p>`;
  }

  function attachRunHandlers(root=document) {
    root.querySelectorAll('[data-run="choice"]').forEach(card => {
      card.querySelector('.check-choice')?.addEventListener('click', () => {
        const block = card.closest('[data-block-id]');
        const id = block?.dataset.blockId;
        const data = findBlockById(id) || window.__singleData;
        const selected = [...card.querySelectorAll('input:checked')].map(i => Number(i.value)).sort((a,b)=>a-b);
        const correct = [...(data.correct || [0])].map(Number).sort((a,b)=>a-b);
        const ok = selected.length === correct.length && selected.every((v,i)=>v===correct[i]);
        card.querySelectorAll('.choice-option').forEach((el,i)=>{ el.classList.toggle('answer-button'); el.classList.toggle('is-correct', correct.includes(i)); el.classList.toggle('is-wrong', selected.includes(i) && !correct.includes(i)); });
        const f = card.querySelector('.feedback'); if (f) { f.hidden=false; f.textContent = ok ? 'Richtig.' : 'Nicht ganz. Die richtige Antwort ist markiert.'; }
      });
    });
    let dragged = null;
    root.querySelectorAll('.chip,.dnd-item').forEach(el => { el.addEventListener('dragstart', () => dragged = el); el.addEventListener('click', () => dragged = el); });
    root.querySelectorAll('.dtw-blank').forEach(blank => { const place=()=>{ if(dragged?.classList.contains('chip')){ blank.textContent=dragged.textContent; blank.dataset.filled=dragged.textContent; dragged.remove(); dragged=null; }}; blank.addEventListener('dragover', e=>e.preventDefault()); blank.addEventListener('drop', place); blank.addEventListener('click', place); });
    root.querySelectorAll('.dnd-target').forEach(zone => { const place=()=>{ if(dragged?.classList.contains('dnd-item')){ zone.appendChild(dragged); dragged=null; }}; zone.addEventListener('dragover', e=>e.preventDefault()); zone.addEventListener('drop', place); zone.addEventListener('click', place); });
    root.querySelectorAll('.check-dtw').forEach(btn => btn.addEventListener('click', () => { const card=btn.closest('[data-run="dragWords"]'); const blanks=[...card.querySelectorAll('.dtw-blank')]; const ok=blanks.length && blanks.every(b=>b.dataset.filled===b.dataset.answer); blanks.forEach(b=>b.classList.toggle('is-correct', b.dataset.filled===b.dataset.answer)); const f=card.querySelector('.feedback'); if(f){f.hidden=false; f.textContent=ok?'Alles richtig.':'Noch nicht alles richtig.';} }));
    root.querySelectorAll('.check-dnd').forEach(btn => btn.addEventListener('click', () => { const card=btn.closest('[data-run="dragDrop"]'); const items=[...card.querySelectorAll('.dnd-target .dnd-item')]; const all=card.querySelectorAll('.dnd-item').length; const ok=items.length===all && items.every(i=>i.dataset.target===i.parentElement.dataset.target); items.forEach(i=>i.classList.toggle('is-correct', i.dataset.target===i.parentElement.dataset.target)); const f=card.querySelector('.feedback'); if(f){f.hidden=false; f.textContent=ok?'Alles richtig.':'Einige Zuordnungen stimmen noch nicht.';} }));
    root.querySelectorAll('.iv-stage').forEach(stage => { const video=stage.querySelector('video'); const overlay=stage.querySelector('.glass-overlay'); let interactions=[]; try{interactions=JSON.parse(stage.dataset.interactions||'[]').map(x=>({...x,done:false}));}catch{} if(!video||!overlay)return; video.addEventListener('timeupdate',()=>{ const item=interactions.find(x=>!x.done && video.currentTime >= Number(x.time||0)); if(!item)return; item.done=true; if(item.pause!==false) video.pause(); overlay.hidden=false; overlay.innerHTML=`<h3>${esc(item.question)}</h3><p>${esc(item.description||'')}</p><div class="answer-stack">${(item.answers||[]).map((a,i)=>`<button class="answer-button" type="button" data-correct="${i===Number(item.correctIndex||0)}">${esc(a)}</button>`).join('')}</div>`; overlay.querySelectorAll('button').forEach(btn=>btn.onclick=()=>{ overlay.hidden=true; video.play().catch(()=>{}); }); }); });
  }

  let state, activePage=0, selectedBlockId=null;
  function findBlockById(id){ if(!state?.pages)return null; return state.pages.flatMap(p=>p.blocks).find(b=>b.id===id); }

  function initContainer(kind) {
    const label = kind === 'book' ? 'Interactive Book' : 'Course Presentation';
    state = { kind, title: label, pages:[defaultPage(1)] };
    selectedBlockId = state.pages[0].blocks[0].id;
    app.innerHTML = `<section class="editor-layout">
      <aside class="panel"><h2>${kind==='book'?'Seiten':'Folien'}</h2><div class="page-list" id="pageList"></div><div class="button-row"><button class="btn primary" id="addPage">${kind==='book'?'Seite':'Folie'} hinzufügen</button><button class="btn danger" id="deletePage">Löschen</button></div></aside>
      <section><div class="canvas-toolbar panel"><div class="field"><label for="activityTitle">Titel</label><input id="activityTitle" type="text"></div><div class="field"><label for="pageTitle">Aktive ${kind==='book'?'Seite':'Folie'}</label><input id="pageTitle" type="text"></div><div class="field"><label for="newBlockType">Element hinzufügen</label><select id="newBlockType"><option value="text">Text</option><option value="imageText">Bild mit Text</option><option value="video">Video</option><option value="interactiveVideo">Interaktives Video</option><option value="choice">Single & Multiple Choice</option><option value="dragWords">Drag the Words</option><option value="dragDrop">Drag and Drop</option></select></div><button class="btn primary" id="addBlock">In aktive ${kind==='book'?'Seite':'Folie'} einfügen</button></div><div class="canvas-wrap"><div class="workspace" id="workspace"></div></div></section>
      <aside class="panel"><h2>Element bearbeiten</h2><div id="props"></div><section class="props-section"><h2>Export / Import</h2><div class="field"><label for="configArea">JSON</label><textarea class="export-area" id="configArea"></textarea></div><div class="button-row"><button class="btn" id="copyJson">JSON anzeigen</button><button class="btn primary" id="downloadZip">HTML-ZIP herunterladen</button><button class="btn" id="importJson">Importieren</button></div></section></aside>
    </section>`;
    wireContainer(); renderContainer();
  }

  function currentPage(){ return state.pages[activePage]; }
  function selectedBlock(){ return findBlockById(selectedBlockId) || currentPage()?.blocks[0]; }
  function wireContainer(){
    app.querySelector('#addPage').onclick=()=>{ state.pages.push(defaultPage(state.pages.length+1)); activePage=state.pages.length-1; selectedBlockId=state.pages[activePage].blocks[0].id; renderContainer(); };
    app.querySelector('#deletePage').onclick=()=>{ if(state.pages.length>1){ state.pages.splice(activePage,1); activePage=Math.max(0,activePage-1); selectedBlockId=state.pages[activePage].blocks[0]?.id; renderContainer(); } };
    app.querySelector('#addBlock').onclick=()=>{ const b=defaultBlock(app.querySelector('#newBlockType').value); currentPage().blocks.push(b); selectedBlockId=b.id; renderContainer(); };
    app.querySelector('#activityTitle').oninput=e=>{ state.title=e.target.value; };
    app.querySelector('#pageTitle').oninput=e=>{ currentPage().title=e.target.value; renderPageTabs(); renderWorkspace(false); };
    app.querySelector('#copyJson').onclick=()=>{ app.querySelector('#configArea').value=JSON.stringify(state,null,2); };
    app.querySelector('#importJson').onclick=()=>{ try{ const imported=JSON.parse(app.querySelector('#configArea').value); state=imported; activePage=0; selectedBlockId=state.pages?.[0]?.blocks?.[0]?.id; renderContainer(); }catch(e){ alert('JSON konnte nicht importiert werden.'); } };
    app.querySelector('#downloadZip').onclick=()=>downloadActivityZip(state, state.kind==='book'?'interactive-book':'course-presentation');
  }
  function renderContainer(){ app.querySelector('#activityTitle').value=state.title||''; app.querySelector('#pageTitle').value=currentPage().title||''; renderPageTabs(); renderWorkspace(); renderProps(); }
  function renderPageTabs(){ const list=app.querySelector('#pageList'); list.innerHTML=state.pages.map((p,i)=>`<button class="page-tab ${i===activePage?'is-active':''}" type="button" data-page="${i}"><span>${esc(p.title)}</span><small>${p.blocks.length}</small></button>`).join(''); list.querySelectorAll('button').forEach(btn=>btn.onclick=()=>{ activePage=Number(btn.dataset.page); selectedBlockId=currentPage().blocks[0]?.id||null; renderContainer(); }); }
  function renderWorkspace(full=true){ const page=currentPage(); const ws=app.querySelector('#workspace'); ws.innerHTML=`<h2 class="workspace-title">${esc(page.title)}</h2>${page.blocks.map(b=>renderBlock(b,selectedBlockId,true)).join('')}`; ws.querySelectorAll('[data-block-id]').forEach(el=>{ el.onclick=(e)=>{ if(e.target.closest('button,input,textarea,select,label'))return; selectedBlockId=el.dataset.blockId; renderWorkspace(); renderProps(); }; el.addEventListener('mouseup',()=>saveSizeFromElement(el)); }); attachRunHandlers(ws); }
  function saveSizeFromElement(el){ const b=findBlockById(el.dataset.blockId); if(!b)return; if(el.style.width) b.style.width=el.style.width; if(el.style.height) b.style.height=el.style.height; }

  function renderProps(){ const b=selectedBlock(); const props=app.querySelector('#props'); if(!b){ props.innerHTML='<p class="hint">Kein Element ausgewählt.</p>'; return; } const s=b.style||defaultStyle(); props.innerHTML=`<div class="field"><label>Titel</label><input id="bTitle" value="${esc(b.title)}"></div><div class="field"><label>Typ</label><select id="bType"><option value="text">Text</option><option value="imageText">Bild mit Text</option><option value="video">Video</option><option value="interactiveVideo">Interaktives Video</option><option value="choice">Single & Multiple Choice</option><option value="dragWords">Drag the Words</option><option value="dragDrop">Drag and Drop</option></select></div><div id="typeProps"></div><section class="props-section"><h2>Darstellung</h2><div class="props-grid"><div class="field"><label>Breite</label><input id="sWidth" value="${esc(s.width)}"></div><div class="field"><label>Höhe</label><input id="sHeight" value="${esc(s.height)}"></div><div class="field"><label>Rahmen</label><input id="sBorder" type="color" value="${esc(s.borderColor)}"></div><div class="field"><label>Fläche</label><input id="sBg" type="color" value="${esc(s.backgroundColor)}"></div><div class="field"><label>Schriftfarbe</label><input id="sText" type="color" value="${esc(s.textColor)}"></div><div class="field"><label>Schriftgröße</label><input id="sSize" type="number" min="10" max="60" value="${esc(s.fontSize)}"></div><div class="field full"><label>Schriftart</label><select id="sFont"><option>Inter, system-ui, sans-serif</option><option>Arial, sans-serif</option><option>Georgia, serif</option><option>Times New Roman, serif</option><option>Verdana, sans-serif</option><option>Courier New, monospace</option></select></div></div></section><div class="button-row"><button class="btn danger" id="deleteBlock">Element löschen</button></div>`;
    props.querySelector('#bType').value=b.type; props.querySelector('#sFont').value=s.fontFamily || 'Inter, system-ui, sans-serif';
    props.querySelector('#bTitle').oninput=e=>{ b.title=e.target.value; renderWorkspace(); };
    props.querySelector('#bType').onchange=e=>{ b.type=e.target.value; b.title=typeLabel(b.type); renderContainer(); };
    ['sWidth','sHeight','sBorder','sBg','sText','sSize','sFont'].forEach(id=>props.querySelector('#'+id).oninput=()=>{ b.style={...b.style,width:props.querySelector('#sWidth').value,height:props.querySelector('#sHeight').value,borderColor:props.querySelector('#sBorder').value,backgroundColor:props.querySelector('#sBg').value,textColor:props.querySelector('#sText').value,fontSize:props.querySelector('#sSize').value,fontFamily:props.querySelector('#sFont').value}; renderWorkspace(); });
    props.querySelector('#deleteBlock').onclick=()=>{ const p=currentPage(); if(p.blocks.length>1){ p.blocks=p.blocks.filter(x=>x.id!==b.id); selectedBlockId=p.blocks[0].id; renderContainer(); } };
    renderTypeProps(b);
  }
  function renderTypeProps(b){ const box=app.querySelector('#typeProps'); const textField=`<div class="field"><label>Text / Beschreibung</label><textarea id="pText">${esc(b.text)}</textarea></div>`; const mediaField=`<div class="field"><label>Medien-URL</label><input id="pMedia" value="${esc(b.media)}" placeholder="https://..."></div>`; if(['text','imageText','video','interactiveVideo'].includes(b.type)) box.innerHTML=textField+(['imageText','video','interactiveVideo'].includes(b.type)?mediaField:''); if(b.type==='choice') box.innerHTML=`<div class="field"><label>Frage</label><textarea id="pQuestion">${esc(b.question)}</textarea></div><div class="field"><label>Zusatztext</label><textarea id="pDesc">${esc(b.description)}</textarea></div><div class="field"><label>Antworten, eine pro Zeile</label><textarea id="pAnswers">${esc(b.answers.join('\n'))}</textarea></div><div class="field"><label>Richtige Antwort(en), Nummern ab 0, mit Komma trennen</label><input id="pCorrect" value="${esc((b.correct||[0]).join(','))}"></div>`; if(b.type==='dragWords') box.innerHTML=`<div class="field"><label>Text mit Ziehwörtern</label><textarea id="pDragText">${esc(b.dragText)}</textarea><p class="hint">Ziehwörter mit [Wort] markieren.</p></div>`; if(b.type==='dragDrop') box.innerHTML=`<div class="field"><label>Anweisung</label><textarea id="pDesc">${esc(b.description||'')}</textarea></div><div class="field"><label>Begriff | Zielbereich</label><textarea id="pPairs">${esc(b.pairs.map(p=>`${p.item} | ${p.target}`).join('\n'))}</textarea></div>`; if(b.type==='interactiveVideo') box.innerHTML=textField+mediaField+`<div class="field"><label>Interaktionen</label><textarea id="pInteractions">${esc(b.interactions.map(x=>`${x.time} | ${x.question} | ${(x.answers||[]).join(';')} | ${x.correctIndex||0}`).join('\n'))}</textarea><p class="hint">Format: Sekunde | Frage | Antwort 1;Antwort 2 | richtige Nummer ab 0</p></div>`; box.querySelectorAll('input,textarea').forEach(el=>el.oninput=()=>{ applyTypeProps(b); if(app.querySelector('#workspace')) renderWorkspace(); else renderSingle(b); }); }
  function applyTypeProps(b){ const q=s=>app.querySelector(s); if(q('#pText')) b.text=q('#pText').value; if(q('#pMedia')) b.media=q('#pMedia').value; if(q('#pQuestion')) b.question=q('#pQuestion').value; if(q('#pDesc')) b.description=q('#pDesc').value; if(q('#pAnswers')) b.answers=q('#pAnswers').value.split('\n').map(x=>x.trim()).filter(Boolean); if(q('#pCorrect')) b.correct=q('#pCorrect').value.split(',').map(x=>Number(x.trim())).filter(Number.isFinite); if(q('#pDragText')) b.dragText=q('#pDragText').value; if(q('#pPairs')) b.pairs=q('#pPairs').value.split('\n').map(line=>{const [item,target]=line.split('|').map(x=>x?.trim()); return item&&target?{item,target}:null;}).filter(Boolean); if(q('#pInteractions')) b.interactions=q('#pInteractions').value.split('\n').map(line=>{const [time,question,answers,correctIndex]=line.split('|').map(x=>x?.trim()); return question?{id:uid(),time:Number(time)||0,question,description:'Wähle eine Antwort aus.',answers:(answers||'Antwort 1').split(';').map(x=>x.trim()).filter(Boolean),correctIndex:Number(correctIndex)||0,pause:true}:null;}).filter(Boolean); }

  function initSingle(kind){ const block = defaultBlock(kind); window.__singleData = block; if(kind==='choice') block.title='Single & Multiple Choice'; if(kind==='dragWords') block.title='Drag the Words'; if(kind==='dragDrop') block.title='Drag and Drop'; if(kind==='interactiveVideo') block.title='Interaktives Video'; app.innerHTML=`<section class="single-layout"><div class="panel single-preview"><h2>Vorschau</h2><div id="singlePreview"></div></div><aside class="panel"><h2>Editor</h2><div id="singleProps"></div><section class="props-section"><h2>Export</h2><div class="field"><label>JSON</label><textarea class="export-area" id="singleJson"></textarea></div><div class="button-row"><button class="btn" id="showSingleJson">JSON anzeigen</button><button class="btn primary" id="downloadSingleZip">HTML-ZIP herunterladen</button></div></section></aside></section>`; selectedBlockId=block.id; state={kind:'single', pages:[{blocks:[block]}]}; renderSingleProps(block); renderSingle(block); app.querySelector('#showSingleJson').onclick=()=>app.querySelector('#singleJson').value=JSON.stringify(block,null,2); app.querySelector('#downloadSingleZip').onclick=()=>downloadActivityZip({kind,title:block.title,pages:[{title:block.title,blocks:[block]}]}, kind); }
  function renderSingle(block){ const p=app.querySelector('#singlePreview'); p.innerHTML=renderBlock(block,block.id,true); attachRunHandlers(p); }
  function renderSingleProps(block){ const box=app.querySelector('#singleProps'); box.innerHTML=`<div class="field"><label>Titel</label><input id="bTitle" value="${esc(block.title)}"></div><div id="typeProps"></div><section class="props-section"><h2>Darstellung</h2><div class="props-grid"><div class="field"><label>Rahmen</label><input id="sBorder" type="color" value="${esc(block.style.borderColor)}"></div><div class="field"><label>Fläche</label><input id="sBg" type="color" value="${esc(block.style.backgroundColor)}"></div><div class="field"><label>Schriftfarbe</label><input id="sText" type="color" value="${esc(block.style.textColor)}"></div><div class="field"><label>Schriftgröße</label><input id="sSize" type="number" value="${esc(block.style.fontSize)}"></div></div></section>`; box.querySelector('#bTitle').oninput=e=>{block.title=e.target.value; renderSingle(block)}; ['sBorder','sBg','sText','sSize'].forEach(id=>box.querySelector('#'+id).oninput=()=>{block.style.borderColor=box.querySelector('#sBorder').value;block.style.backgroundColor=box.querySelector('#sBg').value;block.style.textColor=box.querySelector('#sText').value;block.style.fontSize=box.querySelector('#sSize').value;renderSingle(block)}); renderTypeProps(block); }

  function exportIndex(data){ return `<!doctype html><html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(data.title||'Aktivität')}</title><link rel="stylesheet" href="activity-style.css"></head><body><main class="export-shell"><h1>${esc(data.title||'Aktivität')}</h1><div id="viewer"></div><div class="export-nav"><button id="prev" type="button">Zurück</button><span id="count"></span><button id="next" type="button">Weiter</button></div></main><script src="activity-data.js"></script><script src="activity-runtime.js"></script></body></html>`; }
  function exportData(data){ return `window.ACTIVITY_DATA = ${JSON.stringify(data,null,2)};`; }
  function exportRuntime(){ return `(${runtime.toString()})();`; }
  function exportCss(){ return document.querySelector('style[data-export-css]')?.textContent || `.export-shell{max-width:1100px;margin:0 auto;padding:32px;font-family:Inter,system-ui,sans-serif}.block{margin:0 0 24px;padding:22px;border:1.4px solid #b8c7d6;background:#fff;resize:both;overflow:auto;min-width:240px;min-height:110px}.media{max-width:100%;max-height:420px}.video{width:100%;max-height:520px;background:#111}.choice-stack,.answer-stack{display:grid;gap:12px;margin-top:18px}.choice-option,.answer-button{display:flex;gap:10px;align-items:center;text-align:left;border:1px solid #ccd;padding:13px 14px;background:#fff}.answer-button.is-correct,.is-correct{background:#dcfce7}.answer-button.is-wrong{background:#fee2e2}.feedback{margin-top:18px;padding:14px;background:#e8f2fb}.dtw-blank{display:inline-flex;min-width:110px;min-height:34px;margin:0 5px 8px;padding:5px 9px;border:1.4px dashed #2f5f8f;background:#e8f2fb}.word-bank,.dnd-bank{display:flex;flex-wrap:wrap;gap:12px;margin:22px 0 24px}.chip,.dnd-item{border:1px solid #ccd;background:#fff;padding:10px 14px;border-radius:999px;font-weight:800}.dnd-target-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:18px;margin:24px 0 28px}.dnd-target{min-height:150px;padding:16px;border:1.5px dashed #2f5f8f;background:#e8f2fb}.iv-stage{position:relative}.glass-overlay{position:absolute;left:6%;right:6%;bottom:8%;padding:24px;background:rgba(255,255,255,.75);backdrop-filter:blur(14px);box-shadow:0 18px 40px rgba(17,24,39,.2)}.btn,.export-nav button,.check-choice,.check-dtw,.check-dnd{margin-top:20px;border:1px solid #2f5f8f;background:#2f5f8f;color:#fff;font-weight:850;padding:12px 16px}`; }
  function downloadActivityZip(data, name){ const files=[{name:'index.html',content:exportIndex(data)},{name:'activity-data.js',content:exportData(data)},{name:'activity-runtime.js',content:exportRuntime()},{name:'activity-style.css',content:exportCss()},{name:'README.txt',content:'index.html öffnen. Die Aktivität ist eigenständig und nicht mit Moodle/H5P verbunden.'}]; downloadZip(`${name || 'activity'}-export.zip`, files); }

  function runtime(){
    const DATA=window.ACTIVITY_DATA||{}; const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])); let active=0; const viewer=document.getElementById('viewer');
    function style(b){const s=b.style||{};return `style="width:${esc(s.width||'100%')};height:${esc(s.height||'auto')};border-color:${esc(s.borderColor||'#b8c7d6')};background:${esc(s.backgroundColor||'#fff')};color:${esc(s.textColor||'#111827')};font-size:${Number(s.fontSize)||16}px;font-family:${esc(s.fontFamily||'Inter,system-ui,sans-serif')}"`;}
    function block(b){return `<article class="block" ${style(b)}><h2>${esc(b.title||'Inhalt')}</h2>${content(b)}</article>`;}
    function content(b){const m=(b.media||'').trim(); if(b.type==='imageText')return `${m?`<img class="media" src="${esc(m)}" alt="">`:''}<p>${esc(b.text||'')}</p>`; if(b.type==='video')return `<p>${esc(b.text||'')}</p>${m?`<video class="video" src="${esc(m)}" controls></video>`:''}`; if(b.type==='interactiveVideo')return `${m?`<div class="iv-stage" data-interactions="${esc(JSON.stringify(b.interactions||[]))}"><video class="video" src="${esc(m)}" controls></video><div class="glass-overlay" hidden></div></div>`:'<p>Kein Video.</p>'}`; if(b.type==='choice')return `<h3>${esc(b.question)}</h3><p>${esc(b.description||'')}</p><div class="choice-stack">${(b.answers||[]).map((a,i)=>`<label class="choice-option"><input type="${(b.correct||[]).length>1?'checkbox':'radio'}" name="c" value="${i}"> ${esc(a)}</label>`).join('')}</div><button class="check-choice">Prüfen</button><div class="feedback" hidden></div>`; if(b.type==='dragWords'){let words=[];let html=esc(b.dragText||'').replace(/\[([^\]]+)\]/g,(_,w)=>{words.push(w);return `<span class="dtw-blank" data-answer="${esc(w)}"></span>`});return `<p>${html}</p><div class="word-bank">${words.map(w=>`<button class="chip" draggable="true">${esc(w)}</button>`).join('')}</div><button class="check-dtw">Prüfen</button><div class="feedback" hidden></div>`} if(b.type==='dragDrop'){let targets=[...new Set((b.pairs||[]).map(p=>p.target))];return `<p>${esc(b.description||'')}</p><div class="dnd-bank">${(b.pairs||[]).map(p=>`<button class="dnd-item" draggable="true" data-target="${esc(p.target)}">${esc(p.item)}</button>`).join('')}</div><div class="dnd-target-grid">${targets.map(t=>`<div class="dnd-target" data-target="${esc(t)}"><strong>${esc(t)}</strong></div>`).join('')}</div><button class="check-dnd">Prüfen</button><div class="feedback" hidden></div>`} return `<p>${esc(b.text||'')}</p>`;}
    function render(){const pages=DATA.pages||[{title:DATA.title||'Aktivität',blocks:[DATA]}]; active=Math.max(0,Math.min(active,pages.length-1)); viewer.innerHTML=`<h2>${esc(pages[active].title||'Seite')}</h2>${(pages[active].blocks||[]).map(block).join('')}`; const count=document.getElementById('count'); if(count)count.textContent=pages.length>1?`${active+1} von ${pages.length}`:''; attach();}
    function attach(){ let dragged=null; document.querySelectorAll('.chip,.dnd-item').forEach(el=>{el.ondragstart=()=>dragged=el;el.onclick=()=>dragged=el}); document.querySelectorAll('.dtw-blank').forEach(b=>{const place=()=>{if(dragged?.classList.contains('chip')){b.textContent=dragged.textContent;b.dataset.filled=dragged.textContent;dragged.remove();dragged=null}};b.ondragover=e=>e.preventDefault();b.ondrop=place;b.onclick=place}); document.querySelectorAll('.dnd-target').forEach(z=>{const place=()=>{if(dragged?.classList.contains('dnd-item')){z.appendChild(dragged);dragged=null}};z.ondragover=e=>e.preventDefault();z.ondrop=place;z.onclick=place}); document.querySelectorAll('.check-choice').forEach(btn=>btn.onclick=()=>{const art=btn.closest('.block');const idx=[...viewer.querySelectorAll('.block')].indexOf(art);const b=(DATA.pages?.[active]?.blocks||[DATA])[idx];const sel=[...art.querySelectorAll('input:checked')].map(i=>Number(i.value)).sort((a,b)=>a-b);const cor=[...(b.correct||[0])].map(Number).sort((a,b)=>a-b);const ok=sel.length===cor.length&&sel.every((v,i)=>v===cor[i]);const f=art.querySelector('.feedback');f.hidden=false;f.textContent=ok?'Richtig.':'Nicht ganz.'}); document.querySelectorAll('.check-dtw').forEach(btn=>btn.onclick=()=>{const card=btn.closest('.block');const blanks=[...card.querySelectorAll('.dtw-blank')];const ok=blanks.length&&blanks.every(x=>x.dataset.filled===x.dataset.answer);const f=card.querySelector('.feedback');f.hidden=false;f.textContent=ok?'Alles richtig.':'Noch nicht alles richtig.'}); document.querySelectorAll('.check-dnd').forEach(btn=>btn.onclick=()=>{const card=btn.closest('.block');const items=[...card.querySelectorAll('.dnd-target .dnd-item')];const all=card.querySelectorAll('.dnd-item').length;const ok=items.length===all&&items.every(i=>i.dataset.target===i.parentElement.dataset.target);const f=card.querySelector('.feedback');f.hidden=false;f.textContent=ok?'Alles richtig.':'Einige Zuordnungen stimmen noch nicht.'}); document.querySelectorAll('.iv-stage').forEach(stage=>{const video=stage.querySelector('video'),overlay=stage.querySelector('.glass-overlay');let items=[];try{items=JSON.parse(stage.dataset.interactions||'[]').map(x=>({...x,done:false}))}catch{} video.ontimeupdate=()=>{const it=items.find(x=>!x.done&&video.currentTime>=Number(x.time||0));if(!it)return;it.done=true;video.pause();overlay.hidden=false;overlay.innerHTML=`<h3>${esc(it.question)}</h3><p>${esc(it.description||'')}</p>${(it.answers||[]).map(a=>`<button>${esc(a)}</button>`).join('')}`;overlay.querySelectorAll('button').forEach(b=>b.onclick=()=>{overlay.hidden=true;video.play().catch(()=>{})})}}); }
    document.getElementById('prev')?.addEventListener('click',()=>{active--;render()}); document.getElementById('next')?.addEventListener('click',()=>{active++;render()}); render();
  }
  function makeZip(files){const enc=new TextEncoder();const crcTable=(()=>{let t=[];for(let n=0;n<256;n++){let c=n;for(let k=0;k<8;k++)c=c&1?0xedb88320^(c>>>1):c>>>1;t[n]=c>>>0}return t})();const crc32=b=>{let c=0xffffffff;for(const x of b)c=crcTable[(c^x)&255]^(c>>>8);return(c^0xffffffff)>>>0};const u16=(a,v)=>a.push(v&255,(v>>>8)&255),u32=(a,v)=>a.push(v&255,(v>>>8)&255,(v>>>16)&255,(v>>>24)&255);let parts=[],central=[],offset=0;for(const f of files){const name=enc.encode(f.name),data=enc.encode(f.content),crc=crc32(data),local=[];u32(local,0x04034b50);u16(local,20);u16(local,0);u16(local,0);u16(local,0);u16(local,0);u32(local,crc);u32(local,data.length);u32(local,data.length);u16(local,name.length);u16(local,0);parts.push(new Uint8Array(local),name,data);const cen=[];u32(cen,0x02014b50);u16(cen,20);u16(cen,20);u16(cen,0);u16(cen,0);u16(cen,0);u16(cen,0);u32(cen,crc);u32(cen,data.length);u32(cen,data.length);u16(cen,name.length);u16(cen,0);u16(cen,0);u16(cen,0);u16(cen,0);u32(cen,0);u32(cen,offset);central.push(new Uint8Array(cen),name);offset+=local.length+name.length+data.length}const csize=central.reduce((s,p)=>s+p.length,0),end=[];u32(end,0x06054b50);u16(end,0);u16(end,0);u16(end,files.length);u16(end,files.length);u32(end,csize);u32(end,offset);u16(end,0);return new Blob([...parts,...central,new Uint8Array(end)],{type:'application/zip'})}
  function downloadZip(name,files){const url=URL.createObjectURL(makeZip(files));const a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),800)}

  if(editorType==='course') initContainer('course');
  if(editorType==='book') initContainer('book');
  if(editorType==='choice') initSingle('choice');
  if(editorType==='drag-words') initSingle('dragWords');
  if(editorType==='drag-drop') initSingle('dragDrop');
  if(editorType==='interactive-video') initSingle('interactiveVideo');
})();
